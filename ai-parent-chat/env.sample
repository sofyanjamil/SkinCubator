# Server-side env vars (do NOT prefix with NEXT_PUBLIC_)
# Option A: provide full webhook URL (recommended for production)
N8N_CHAT_WEBHOOK_URL=
# Option B: provide base URL and path (legacy)
N8N_BASE_URL=http://localhost:5679
N8N_CHAT_WEBHOOK_PATH=/webhook-test/b0c6d818-0aea-4787-9de3-ef130797ade2

# Database connection (required to persist messages)
# Option A: single URL
# DATABASE_URL=postgresql://skincubator:skincubator@localhost:5432/skincubator?sslmode=disable
# Option B: discrete params
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=skincubator
# PGUSER=skincubator
# PGPASSWORD=skincubator
# PGSSLMODE=disable

# Optional: override cookie name for session persistence
SESSION_COOKIE_NAME=sc_session
# Optional: cookie max age (in seconds); default ~90 days
SESSION_COOKIE_MAX_AGE=7776000

# Client-side env vars (exposed to browser)
NEXT_PUBLIC_APP_NAME=SkinCubator
